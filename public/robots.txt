# robots.txt — villadeepcleaning.com
#
# Only internal/non-content paths are blocked. Legacy WordPress URLs from the
# previous site on this domain are intentionally NOT blocked here so Googlebot
# can crawl them, see their 301-redirect / 404 (handled in next.config.ts), and
# drop them cleanly from the index. Blocking them would prevent Google from
# processing the removal.

User-agent: *
Allow: /

Disallow: /api/
Disallow: /_next/

Sitemap: https://www.villadeepcleaning.com/sitemap.xml
